{{ ansible_managed | comment }}
# Rendered by Ansible from roles/app/templates/web.env.production.j2.
#
# Vite reads this at BUILD time and inlines the value into the JavaScript
# bundle. Changing it later and restarting anything does nothing — the frontend
# must be rebuilt. That is why roles/app renders this file before `npm run
# build` and then greps the emitted assets to prove the value made it in.
VITE_API_BASE_URL={{ frontend_api_base_url }}
